// Nothing here assigns hp, gold or playerName — the passage does.
// This file only READS them, which is the point of part B.
const remaining: number = State.variables.hp;
const label: string = State.variables.playerName;
const neverSetAnywhere = State.variables.notAssignedInAnyPlace;
