const good = setup.attack(5);
const bad = setup.attack('nope');
const unsetVar = State.variables.neverAssignedInTypeScript;
const unsetMember = setup.neverAssignedInTypeScript;
const unsetSetting = settings.neverAssignedInTypeScript;
